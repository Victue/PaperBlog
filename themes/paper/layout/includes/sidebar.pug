.sidebar
  //- 默认参数 true
  if !page.posts && (theme.toc === undefind || theme.toc === true)
    include ./toc

  if theme.link === undefind || theme.link === true
    h2 Links
    .sidebar__link
      if theme.social
        ul
          each url,label in theme.social
            li
              a(href=url_for(url)) #{label}

  if theme.archives === undefind || theme.archives === true
    h2 Archives
    .sidebar__archives
      != list_archives({type:"yearly", format: "YYYY", show_count: false})

  if theme.categories === undefind || theme.categories === true
    h2 Categories
    .sidebar__categories
      != list_categories({show_count: false})

  if theme.tags === undefind || theme.tags === true
    h2 Tags
    .sidebar__tags
      != list_tags({show_count: false})

  iframe src="https://www.random.org/widgets/integers/iframe.php?title=True+Random+Integer+Generator&amp;buttontxt=Generate&amp;width=160&amp;height=200&amp;border=on&amp;bgcolor=%23FFFFFF&amp;txtcolor=%23777777&amp;altbgcolor=%23CCCCFF&amp;alttxtcolor=%23000000&amp;defaultmin=&amp;defaultmax=&amp;fixed=off" frameborder="0" width="160" height="200" style="min-height:200px;" scrolling="no" longdesc="https://www.random.org/integers/"
	  p The numbers generated by this widget come from RANDOM.ORG's true random number generator.

